# Minimal requirements to run the loss-landscape & evaluation scripts in this repo
# Adjust versions to match your CUDA / platform as needed.

# Core ML stack
torch>=2.1.0
transformers>=4.35.0
datasets>=2.12.0
huggingface-hub>=0.15.0
accelerate>=0.20.0

# LM evaluation harness
lm-eval>=0.3.3

# Tokenizers & tokenization backends
sentencepiece
tokenizers

# Utilities
numpy>=1.23
matplotlib>=3.5
pyyaml>=6.0

# Optional / project-specific
# GaLore optimizer (if you want to run GaLore experiments)
galore-torch>=0.1.0

# For reproducible installs you may want to pin exact versions above once you validate on your machine

# for loss landscape
fastchat
shortuuid
fschat
openai
anthropic